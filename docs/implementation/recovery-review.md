# Recovery review

The Astra-medium Spec review inspected committed `cb78b64...6c221b6` on
2026-09-10. The initial public recovery test had an observed unsupported
`rebuild` RED before implementation. Independent verification of the committed
slice passed typecheck, build and all 55 tests using the pinned dependencies.

The reviewer reproduced three recovery defects through public operations:

1. Legitimate older or partially published projections were diagnosed as
   manual edits after cache loss or `PROJECTION_FAILED`. Rebuild must recognize
   generated bytes from validated historical states and their commit times,
   including snapshots older than the latest semantic revision.
2. A disposable manifest digest could be changed to match a manual projection
   edit, allowing rebuild to silently overwrite that edit. Cache hashes must
   not establish authenticity; accepted generated bytes come from history.
3. Malformed JSON in the disposable projection manifest prevented rebuild,
   even when history and projections were intact. Rebuild must regenerate
   malformed disposable metadata while retaining filesystem containment and
   manual-edit protection.

Commit `c736575` corrects all three findings. Stale-projection and malformed
manifest behavior each had an observed public RED before its minimum GREEN.
The historical-byte authenticity fix also made the manipulated-cache-hash and
post-commit recovery checks pass immediately; those are recorded as
verification rather than historical TDD. All five recovery tests passed.

Both Astra review axes found no remaining material finding in these bounded
changes. The Standards review accepted the lock structure for its existing
nested call path, but no deterministic concurrency experiment was claimed.
Commit `d111a6c` completes the separate Git-ignore requirement. A public test
first failed because the disposable index was not ignored, then passed after
projection creation restored a managed `.justification/.gitignore`. The test
uses Git's ignore engine, preserves existing root ignore bytes, checks that
history and generated knowledge remain eligible for tracking, and repeats
after deleting the whole cache. Both Astra review axes found no material
finding in this correction. All six recovery tests passed; parent combined
typecheck and 70-test verification also passed.
